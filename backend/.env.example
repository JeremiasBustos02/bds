# Variables de entorno obligatorias — la app NO arranca sin ellas

# Base de datos (PostgreSQL)
DB_PASSWORD=
# DB_URL=jdbc:postgresql://localhost:5432/mitienda

# JWT — generar con: openssl rand -base64 32
JWT_SECRET=

# Google OAuth2 (obligatorio para login con Google)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
